# Golden screenshot fixtures

The files in this directory are generated from `/control-plane?state=0..8` at `1440×900`, one image per canonical state. Regenerate them with `npm run capture` after a visual-system or projection change.

They are presentation fixtures, not evidence of Route A integration or real Cloud execution.
